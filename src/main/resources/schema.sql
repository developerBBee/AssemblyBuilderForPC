CREATE TABLE IF NOT EXISTS pccase (
  id VARCHAR(32) PRIMARY KEY,
  url VARCHAR(100),
  name VARCHAR(100),
  imgurl VARCHAR(200),
  type VARCHAR(10),
  price INT,
  rank INT
);
CREATE TABLE IF NOT EXISTS motherboard (
  id VARCHAR(32) PRIMARY KEY,
  url VARCHAR(100),
  name VARCHAR(100),
  imgurl VARCHAR(200),
  type VARCHAR(10),
  price INT,
  rank INT
);
CREATE TABLE IF NOT EXISTS powersupply (
  id VARCHAR(32) PRIMARY KEY,
  url VARCHAR(100),
  name VARCHAR(100),
  imgurl VARCHAR(200),
  type VARCHAR(10),
  price INT,
  rank INT
);
CREATE TABLE IF NOT EXISTS cpu (
  id VARCHAR(32) PRIMARY KEY,
  url VARCHAR(100),
  name VARCHAR(100),
  imgurl VARCHAR(200),
  type VARCHAR(10),
  price INT,
  rank INT
);
CREATE TABLE IF NOT EXISTS cpucooler (
  id VARCHAR(32) PRIMARY KEY,
  url VARCHAR(100),
  name VARCHAR(100),
  imgurl VARCHAR(200),
  type VARCHAR(10),
  price INT,
  rank INT
);
CREATE TABLE IF NOT EXISTS pcmemory (
  id VARCHAR(32) PRIMARY KEY,
  url VARCHAR(100),
  name VARCHAR(100),
  imgurl VARCHAR(200),
  type VARCHAR(10),
  price INT,
  rank INT
);
CREATE TABLE IF NOT EXISTS hdd35inch (
  id VARCHAR(32) PRIMARY KEY,
  url VARCHAR(100),
  name VARCHAR(100),
  imgurl VARCHAR(200),
  type VARCHAR(10),
  price INT,
  rank INT
);
CREATE TABLE IF NOT EXISTS ssd (
  id VARCHAR(32) PRIMARY KEY,
  url VARCHAR(100),
  name VARCHAR(100),
  imgurl VARCHAR(200),
  type VARCHAR(10),
  price INT,
  rank INT
);
CREATE TABLE IF NOT EXISTS videocard (
  id VARCHAR(32) PRIMARY KEY,
  url VARCHAR(100),
  name VARCHAR(100),
  imgurl VARCHAR(200),
  type VARCHAR(10),
  price INT,
  rank INT
);
CREATE TABLE IF NOT EXISTS ossoft (
  id VARCHAR(32) PRIMARY KEY,
  url VARCHAR(100),
  name VARCHAR(100),
  imgurl VARCHAR(200),
  type VARCHAR(10),
  price INT,
  rank INT
);
CREATE TABLE IF NOT EXISTS lcdmonitor (
  id VARCHAR(32) PRIMARY KEY,
  url VARCHAR(100),
  name VARCHAR(100),
  imgurl VARCHAR(200),
  type VARCHAR(10),
  price INT,
  rank INT
);
CREATE TABLE IF NOT EXISTS keyboard (
  id VARCHAR(32) PRIMARY KEY,
  url VARCHAR(100),
  name VARCHAR(100),
  imgurl VARCHAR(200),
  type VARCHAR(10),
  price INT,
  rank INT
);
CREATE TABLE IF NOT EXISTS mouse (
  id VARCHAR(32) PRIMARY KEY,
  url VARCHAR(100),
  name VARCHAR(100),
  imgurl VARCHAR(200),
  type VARCHAR(10),
  price INT,
  rank INT
);
CREATE TABLE IF NOT EXISTS dvddrive (
  id VARCHAR(32) PRIMARY KEY,
  url VARCHAR(100),
  name VARCHAR(100),
  imgurl VARCHAR(200),
  type VARCHAR(10),
  price INT,
  rank INT
);
CREATE TABLE IF NOT EXISTS bluraydrive (
  id VARCHAR(32) PRIMARY KEY,
  url VARCHAR(100),
  name VARCHAR(100),
  imgurl VARCHAR(200),
  type VARCHAR(10),
  price INT,
  rank INT
);
CREATE TABLE IF NOT EXISTS soundcard (
  id VARCHAR(32) PRIMARY KEY,
  url VARCHAR(100),
  name VARCHAR(100),
  imgurl VARCHAR(200),
  type VARCHAR(10),
  price INT,
  rank INT
);
CREATE TABLE IF NOT EXISTS pcspeaker (
  id VARCHAR(32) PRIMARY KEY,
  url VARCHAR(100),
  name VARCHAR(100),
  imgurl VARCHAR(200),
  type VARCHAR(10),
  price INT,
  rank INT
);
CREATE TABLE IF NOT EXISTS fancontroller (
  id VARCHAR(32) PRIMARY KEY,
  url VARCHAR(100),
  name VARCHAR(100),
  imgurl VARCHAR(200),
  type VARCHAR(10),
  price INT,
  rank INT
);
CREATE TABLE IF NOT EXISTS casefan (
  id VARCHAR(32) PRIMARY KEY,
  url VARCHAR(100),
  name VARCHAR(100),
  imgurl VARCHAR(200),
  type VARCHAR(10),
  price INT,
  rank INT
);
